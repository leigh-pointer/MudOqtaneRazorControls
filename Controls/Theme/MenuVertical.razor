@using System.Collections.Generic
@using System.Linq
@using Oqtane.Models
@inherits MenuBase

<MudNavMenu>
    @foreach (var menuPageItem in MenuPages.Where(p => p.ParentId == null))
    {
        <MenuItemVertical MenuItemData="menuPageItem" />
    }
</MudNavMenu>